version: v1.0
name: develop-deploy
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004

blocks:
  - name: develop-deploy
    task:
      secrets:
        - name: heroku-http
      env_vars:
        - name: HEROKU_APP_NAME
          value: rails-bootstrap-ui-develop
      jobs:
        - name: develop-deploy
          commands:
            - checkout --use-cache
            - heroku git:remote -a $HEROKU_APP_NAME
            - git push heroku -f $SEMAPHORE_GIT_BRANCH:master
            - if heroku run -x rails db:migrate; then heroku dyno:restart; else heroku rollback; exit -1; fi
