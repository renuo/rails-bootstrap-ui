<% button_class = "#{outline ? 'btn-outline' : 'btn'}-#{theme}" %>
<% button_class += ' active' if active %>

  <button type='button' class='btn <%= button_class %>' <%= 'disabled' if disabled %>>
    <%= title %>
  </button>
