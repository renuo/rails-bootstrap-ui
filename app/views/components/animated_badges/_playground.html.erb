<div class="pulsating-badge bg-<%= colour %>"
     style="--border-color: <%= border_color %>; animation-duration: <%= duration %>s; --pulse-scale: <%= pulse_scale.to_f %>;">
  <span class="badge">
    <%= title %>
  </span>
</div>
